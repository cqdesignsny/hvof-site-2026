# HVOF site environment variables
# Copy this to .env.local for local development.
# In production, set these via `vercel env add` or the Vercel dashboard.

# --- Public (browser-exposed) ---

# Google Analytics 4 measurement ID (looks like G-XXXXXXXXXX)
NEXT_PUBLIC_GA_ID=

# Meta (Facebook) Pixel ID — numeric only
NEXT_PUBLIC_FB_PIXEL_ID=

# Optional: override the canonical site URL for metadata + sitemap.
# Defaults to value in src/lib/site.ts when not set.
# NEXT_PUBLIC_SITE_URL=https://hvof-site-2026.vercel.app

# --- Server-only (do NOT prefix with NEXT_PUBLIC) ---

# Resend transactional email (used by /api/lead route)
RESEND_API_KEY=
LEAD_EMAIL_TO=cesar@creativequalitymarketing.com
LEAD_EMAIL_FROM="HVOF Site <noreply@thewowguys.com>"

# Optional: Typeform embed URL — used in contact CTA fallback
NEXT_PUBLIC_TYPEFORM_URL=

# --- Floorplan admin (the /admin backend) ---

# Single shared password for the /admin gate. Pick something strong.
# To set on Vercel use printf to avoid trailing newlines:
#   printf "value" | vercel env add ADMIN_PASSWORD production
ADMIN_PASSWORD=

# Long random string used to sign the admin session cookie. 32+ characters.
# Generate with: openssl rand -hex 32
ADMIN_SESSION_SECRET=

# --- Lead pipeline (Neon Postgres, HVOF-DB) ---
# Provisioned via Vercel Marketplace. These are auto-populated for Production
# and Preview. Local dev falls back to the in-memory store unless you also
# attach the Neon resource to the Development environment, or copy these
# values manually.
DATABASE_URL=
DATABASE_URL_UNPOOLED=
# (POSTGRES_*, PG*, NEON_PROJECT_ID also auto-populated by the integration)

# --- CQ Signal data plane (read-only consumer) ---
# When both are set, /admin/reports pulls from Signal's v1 REST API instead of
# the local mock, and /api/lead fires a fire-and-forget POST to Signal's
# /api/v1/leads/ingest after each successful local lead write.
# Leave both unset to keep Floorplan in mock-data mode while the Signal-side
# build is in flight. See SIGNAL-HANDOFF.md in the Dropbox project root.
SIGNAL_API_BASE=
SIGNAL_API_KEY=
